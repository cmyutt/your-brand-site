import prisma from "@/lib/prisma";export async function recordAdminLog(entry: {  targetType: string;  targetId: string;  action: string;  actor?: string | null;  note?: string | null;  snapshot?: unknown;}) {  try {    await prisma.adminLog.create({      data: {        targetType: entry.targetType,        targetId: entry.targetId,        action: entry.action,        actor: entry.actor ?? undefined,        note: entry.note ?? undefined,        // snapshot?� 민감?�보 최소?�해???�기 권장        snapshot: entry.snapshot as any,      },    });  } catch {    /* noop */  }}